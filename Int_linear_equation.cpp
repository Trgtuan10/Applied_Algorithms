#include<bits/stdc++.h>

using namespace std; 

int n, int m;
int a[1000];
int c;
int cur = 0;

void Try(int k){
    for(int i=a[k-1]; i<= n; i++){
        a[k] = i;
        cur += i; 
        if(k == n-1 && (n-cur) % x[n]) 

    }
}